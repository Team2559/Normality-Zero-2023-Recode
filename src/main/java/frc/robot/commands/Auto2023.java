package frc.robot.commands;

import edu.wpi.first.wpilibj2.command.CommandBase;
import edu.wpi.first.wpilibj2.command.Commands;
import frc.robot.subsystems.AutoSubsystem;
import frc.robot.subsystems.ShooterSubsystem;
import frc.robot.commands.ShootCommand;


public class Auto2023 {
    /*private AutoSubsystem m_AutoSubsystem;

    public Auto2023(AutoSubsystem subsystem) {
        m_AutoSubsystem = subsystem;

        addRequirements(subsystem);
    }

    public static CommandBase autoStart(AutoSubsystem m_AutoSub) {

         //return Commands.sequence(m_AutoSub.autoMethodCommand(null));
         //return Commands.sequence(subsystem.exampleMethodCommand(), new ExampleCommand(subsystem));
       
        return Commands.sequence(AutoSubsystem.autoMethodCommand(m_AutoSub), new ExampleCommand(m_AutoSub));
        }

       */

}
